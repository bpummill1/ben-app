language: node_js

node_js:
  - 7.4.0

build:
  ci:
    - npm install
    - npm run build
    - npm run test
  post-ci:
    - docker build -t bens-app .

integrations:
    notifications:
      - integrationName: shippable-build-status
        type: slack
        recipients: "#ben-app-shippable"
